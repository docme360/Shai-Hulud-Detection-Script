# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

custom-shai-hulud is a Python project using Python 3.11+.

## Development Setup

This project uses `uv` for dependency management (indicated by pyproject.toml structure).

```bash
# Install dependencies
uv sync

# Run the application
uv run python main.py
```

## Project Structure

- `main.py` - Application entry point
- `pyproject.toml` - Project configuration and dependencies
